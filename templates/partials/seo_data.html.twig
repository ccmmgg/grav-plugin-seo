{#
  DEPRECATED — this partial is no longer the canonical output path.

  All meta tags and JSON-LD structured data are now emitted directly by the
  PHP plugin (SeoPlugin::onPageInitialized / onOutputGenerated). Including
  this partial while the plugin is active will produce duplicate tags.

  This file is retained as an empty stub so themes that reference it do not
  throw a Twig "template not found" error. It may be removed in a future
  major version.
#}
